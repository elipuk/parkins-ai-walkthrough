# Agent instructions

The conventions for working in this repo live in **[CLAUDE.md](CLAUDE.md)**.
Read that before making changes.

Codex reads `AGENTS.md` and Claude reads `CLAUDE.md`, but they govern the same
repo and the same work, so there is one source and this is a pointer to it.
Do not copy the rules here — two copies drift, and then the repo has two rules
and no way to tell which one is current.
